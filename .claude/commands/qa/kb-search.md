---
description: 跨产品本地 + Confluence + Jira 检索
argument-hint: <keyword> [--product=xxx] [--jira]
allowed-tools: Read, Glob, Grep, mcp__atlassian__*
---

# /qa:kb-search $ARGUMENTS

按 `prompts/kb-search.md` 的完整规范执行：在 `_kb/` 本地 + Confluence + (可选)Jira 中检索关键词，列出命中段落与页面。

读取并严格遵循：
@prompts/kb-search.md
@CLAUDE.md
